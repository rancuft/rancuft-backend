server:
  port: 8082
spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3307/rancuft?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: ${PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql